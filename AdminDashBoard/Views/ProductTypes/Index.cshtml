@model IEnumerable<BrandAndTypeViewModel>
@{
    ViewData["Title"] = "Index";
}


<h1>All Types</h1>
<hr />
<partial name="CreateBrandPartialView" model="new CreatedBrandAndTypeViewModel()" />

@if (Model?.Count() > 0)
{
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(r => r.Id)</th>
                <th>@Html.DisplayNameFor(r => r.Name)</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var Type in Model)
            {
                <tr class="align-middle">
                    <td class="fw-semibold">@Type.Id</td>

                    <td class="fw-semibold">@Type.Name</td>
                    <td>
                        <a class="btn btn-sm btn-warning text-white me-1" asp-action="Edit" asp-route-id="@Type.Id">
                            <i class="fas fa-edit"></i>
                        </a>

                        <a class="btn btn-sm btn-danger text-white"
                           href="javascript:void(0);"
                           onclick="confirmDelete('@Url.Action("Delete", new { id = Type.Id })')">
                            <i class="fa-solid fa-trash"></i>
                        </a>
                    </td>
                </tr>

            }
        </tbody>
    </table>
}
else
{
    <div class="mt-3 p-3 alert-warning">
        <h2>There is no Products</h2>

    </div>
}
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        function confirmDelete(url) {
            if (confirm("Are you sure you want to delete this Type?")) {
                window.location.href = url;
            }
        }
    </script>
}